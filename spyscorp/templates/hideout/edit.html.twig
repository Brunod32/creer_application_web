{% extends 'base.html.twig' %}

{% block title %}Modifier une planque{% endblock %}

{% block body %}
    {{ include('_navbar.html.twig') }}
    <div class="container-fluid">
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header text-center mt-2">Modifier une planque</h1>
            </div>
        </div>
    </div>

    <div class="m-5">
        {{ include('hideout/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('hideout_index') }}">Retour à la liste</a>

        {{ include('hideout/_delete_form.html.twig') }}
    </div>
{% endblock %}
