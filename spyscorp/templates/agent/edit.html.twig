{% extends 'base.html.twig' %}

{% block title %}Modifier un agent{% endblock %}

{% block body %}
    {{ include('_navbar.html.twig') }}
    <div class="container-fluid">
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header text-center mt-2">Modifier un agent</h1>
            </div>
        </div>
    </div>

    <div class="m-5">
        {{ include('agent/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('agent_index') }}">Retour à la liste</a>

        {{ include('agent/_delete_form.html.twig') }}
    </div>
{% endblock %}
