{% extends 'base.html.twig' %}

{% block title %}Modifier un contact{% endblock %}

{% block body %}
    {{ include('_navbar.html.twig') }}
    <div class="container-fluid">
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header text-center mt-2">Modifier un contact</h1>
            </div>
        </div>
    </div>

    <div class="m-5">
        {{ include('contact/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('contact_index') }}">Retour à lal liste</a>

        {{ include('contact/_delete_form.html.twig') }}
    </div>
{% endblock %}
