{% extends 'base.html.twig' %}

{% block title %}Modifier une cible{% endblock %}

{% block body %}
    {{ include('_navbar.html.twig') }}
    <div class="container-fluid">
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header text-center mt-2">Modifier une cible</h1>
            </div>
        </div>
    </div>

    <div class="m-5">
        {{ include('target/_form.html.twig', {'button_label': 'Modifier'}) }}

        <a href="{{ path('target_index') }}" class="btn btn-success mt-2">Retour à la liste</a>

        {{ include('target/_delete_form.html.twig') }}
    </div>

    {{ include('_footer.html.twig') }}
{% endblock %}
