{% extends 'base.html.twig' %}

{% block title %}Nouvel agent{% endblock %}

{% block body %}
    {{ include('_navbar.html.twig') }}
    <div class="container-fluid">
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header text-center mb-5 mt-2">Créer un nouvel agent</h1>
            </div>
        </div>

        <div class="m-5">
            {{ include('agent/_form.html.twig') }}

            <a href="{{ path('agent_index') }}" role="button" class="btn btn-success mt-2">Retour à la liste</a>
        </div>
    </div>


{% endblock %}
