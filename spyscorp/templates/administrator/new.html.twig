{% extends 'base.html.twig' %}

{% block title %}Nouvel administrateur{% endblock %}

{% block body %}
    {{ include('_navbar.html.twig') }}
    <div class="container-fluid">
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header text-center mb-5 mt-2">Créer un nouvel administrateur</h1>
            </div>
        </div>

        <div class="m-5">
            {{ include('administrator/_form.html.twig') }}

            <a href="{{ path('administrator_index') }}" role="button" class="btn btn-success mt-2">Retour à la liste</a>
        </div>
    </div>

{% endblock %}
